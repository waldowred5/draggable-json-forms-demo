import { rankWith, uiTypeIs } from '@jsonforms/core';

export const sectionLayoutTester = rankWith(1000, uiTypeIs('Categorization'));
// export const sectionLayoutTester = rankWith(1000, uiTypeIs('Group'));
